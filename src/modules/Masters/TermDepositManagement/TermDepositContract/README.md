# TermDepositContract

Screen automation for TermDepositContract.

## Structure

- `src/` — Page, Repository, Builder, Validator
- `tests/` — create, update, authorize, negative, db specs
- `data/` — Excel test data
